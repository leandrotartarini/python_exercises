width = float(input('Walls width: '))
height = float(input('Walls height: '))
area = width * height
ink = area / 2
print(f'Walls dimession {width}x{height} and area {area}m2')
print(f'To paint this wall, you will need {ink} of ink')